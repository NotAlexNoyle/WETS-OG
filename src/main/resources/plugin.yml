name: WETS-OG
version: ${version}
website: https://github.com/NotAlexNoyle/WETS-OG
main: ar.emily.wets.bukkit.WESpreadPlugin
api-version: 1.19
depend:
  - WorldEdit

commands:
  worldedit-tick-spreader:
    usage: /<command> (<blocks per tick> | sorted | not-sorted)
    permission: wets
    aliases:
      - wets

permissions:
  wets:
    default: op
